# 参与贡献 · Contributing

首先，谢谢你有兴趣来折腾这个项目。下面几件事说清楚，省得来回踩坑。

---

## 一、这是个什么性质的项目

在动手之前请先读一遍 [DISCLAIMER.md](./DISCLAIMER.md)。

一句话：**这是开源的教学 / 架构参考项目，不是生产级防汛系统。** 它用来学"防汛预警系统大概怎么搭"，不能拿去做真实的撤离决策。所以：

- 不要往里塞**真实的降雨 / 水文 / 人口 / 工程数据**，示例数据都是合成脱敏的；
- 不要为了"看起来更真"去**弱化或删改免责声明**；
- 任何算法 / 模型改动，请默认它仍是**示意性**的，别宣称已业务化标定。

---

## 二、本地怎么跑起来

环境要求：Python 3.11+，能联网（前端地图和图表走公共 CDN）。

```bash
# 1) 看可交互演示（自动起本地服务并开浏览器）
python start.py

# 2) 直接跑算法引擎自带的演示例子
python algorithms/flood_risk_engine.py
```

不需要装任何第三方 Python 包，算法引擎只用标准库。

---

## 三、项目结构（改之前先认路）

```
系统设计方案.md          技术方案主文档（九章 + 完善增强篇）
algorithms/              核心算法（DFRI / 马斯京根 / 概率化 / 数据质量 / 验证 / 预警闭环）
data/                   三城八试点的合成模拟数据
demo/                   可交互演示前端（单文件 index.html，纯原生 JS）
start.py                一键启动器
```

---

## 四、代码风格

**Python（`algorithms/`）**

- 优先用标准库，别轻易加依赖（保持"能直接跑"）；
- 函数 / 类加类型注解，关键计算步骤写清参数名与单位；
- 注释可以用中文，但请写"为什么"而不是复述代码；
- 新增算法类请带一个可运行的 `run_*` 演示，方便别人验证。

**前端（`demo/`）**

- 保持**单文件、零构建**：原生 HTML/CSS/JS，不引入打包工具；
- 第三方库只走公共 CDN（Leaflet / ECharts），别 vendoring 大文件；
- 改样式走 CSS 变量，暗色主题保持一致。

**文档（`系统设计方案.md`）**

- 改公式 / 接口时，顺手同步更新对应章节与 Mermaid 图；
- 新增能力建议在"完善增强篇"对应小节补，而不是散落各处。

---

## 五、提交与 PR

1. Fork 后开分支开发，分支名随意但有语义（如 `feat/xxx`、`fix/xxx`）；
2. Commit message 直述事实，别写"可能应该""我觉得"这类犹豫腔；
3. 提 PR 前自己先 `python algorithms/flood_risk_engine.py` 跑一遍确认没报错；
4. PR 描述说清：**改了什么、为什么、怎么验证**；
5. 涉及免责声明 / 许可的改动，请特别说明理由。

---

## 六、提 Issue

- Bug：给复现步骤 + 环境（Python 版本、浏览器）；
- 想法 / 改进：先聊清楚再写代码，避免白做；
- 安全相关：如果真发现模型有严重误用风险，请直接提 Issue 标注，我们会优先处理。

---

## 七、许可证

本项目以 [MIT](./LICENSE) 许可证开源。提交即表示你同意你的贡献在相同许可下发布。

> 再次提醒：开源不等于无责。拿去实际部署，请自行完成标定、合规与责任承担（详见 DISCLAIMER.md）。
